# Action - `{{ cnclrexorder }}`

## Description

The `{{ cnclrexorder }}` action cancels the queued REX sell order if one exists.

As an authorized party I {{ signer }} wish to cancel queued REX sell order if one exists from the account {{ owner }}.
